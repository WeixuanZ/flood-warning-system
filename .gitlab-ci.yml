image: python:3.7

test:
  script:
  # Install Python packages required to run code
  - pip install dateutils pytest requests numpy matplotlib

    # Run tests
  - python -m pytest -v .

  # Run deliverables
  - python Task1A.py

  - python Task2A.py
  - python Task2D.py
